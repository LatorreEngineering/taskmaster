{
  "name": "taskmaster-backend",
  "version": "1.0.0",
  "description": "Backend API for TaskMaster",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0"
  }
}
```

2. **Create `backend/data/` folder** (for the SQLite database)

3. **Create `.gitignore` at root:**
```
node_modules/
.env
*.db
.DS_Store
